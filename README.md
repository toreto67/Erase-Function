# Runtime Eraser
A simple function eraser.

Created to bypass in memory patches. (by just removing the function entirely)

This is detected on most anticheats (since we overwrite page protections) but it is still a great tool to remove functions that may be used to reverse your loader in runtime. (ex: authentication)
